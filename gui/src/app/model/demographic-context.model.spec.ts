import { DemographicContext } from './demographic-context.model';

describe('DemographicContext', () => {
  it('should create an instance', () => {
    expect(new DemographicContext()).toBeTruthy();
  });
});
